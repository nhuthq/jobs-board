<p style="color: green"><%= notice %></p>
<% content_for :page_title, @organization.name %>
<div class="card">
  <div class="card-header">
    <h1>Edit Organization</h1>
  </div>
  <div class="card-body">
    <%= render 'form', organization: @organization %>
  </div>
</div>

<br/>
<div>
  <%= link_to "Back", organizations_path, class: "btn btn-secondary" %>
</div>
<br/>